//Question1_Declare an empty array;

const arr = Array()

console.log(arr) 
//One Way Declare empty array
let array = [];
//2nd Way You can use the Array() constructor to create an empty array.
const myArray = new Array();
//3rd Way You can use the Array.from() method to create an empty array.
const myarray = array.from([]);